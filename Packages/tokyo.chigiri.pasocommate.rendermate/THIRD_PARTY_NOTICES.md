# Third Party Notices

このディレクトリ配下のシェーダーには、`com.llealloo.audiolink` の
`AudioLink.cginc` を参照する実装が含まれます。

- Dependency package: `com.llealloo.audiolink`
- Referenced include path:
  - `Packages/com.llealloo.audiolink/Runtime/Shaders/AudioLink.cginc`

本ディレクトリは AudioLink 本体コードを同梱していません。
依存関係は利用側環境で解決される前提です。
